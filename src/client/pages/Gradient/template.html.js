import * as React from 'react';
import * as _ from 'lodash';
export default function () {
    return React.createElement('div', { 'className': 'Gradient' }, React.createElement('div', { 'className': 'category flex columns center centered' }, React.createElement('p', {}, 'Gradient')));
}